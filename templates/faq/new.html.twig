{% extends 'base.html.twig' %}

{% block title %}New Faq{% endblock %}

{% block body %}
    <div class="faq-action-btn">
        <a href="{{ path('faq_index') }}"><button class="btn btn-round-darkblue">back to list</button></a>
        <span class="empty"></span>
    </div>

    {{ include('faq/_form.html.twig') }}
{% endblock %}
